import 'package:flutter/material.dart';

class UIConstants {
  // Border radius values
  static const BorderRadius smallRadius = BorderRadius.all(Radius.circular(8));
  static const BorderRadius midRadius = BorderRadius.all(Radius.circular(12));
  static const BorderRadius largeRadius = BorderRadius.all(Radius.circular(14));

  // Padding values
  static const EdgeInsets horizontalPadding = EdgeInsets.symmetric(
    horizontal: 24,
  );
  static const EdgeInsets verticalPadding12 = EdgeInsets.symmetric(
    vertical: 12,
  );
  static const EdgeInsets verticalPadding16 = EdgeInsets.symmetric(
    vertical: 16,
  );
}
